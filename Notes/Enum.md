A way of giving more friendly names to integer values.

Useful when a variable can only take one out of a small set of possible values.

## Example:
```ts
enum Color { Red, Green, Blue }
let c: Color = Color.Green;
```